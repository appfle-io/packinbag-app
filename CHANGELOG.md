# 팩인백 (Packinbag) 릴리즈 노트

## [1.0.13] - 2026-09-21

### 버그 수정 및 개선 사항
- **macOS 포터블 앱 실행 크래시(Trace/BPT trap: 5) 해결**:
  - `electron-builder` 설정 내 `productName`을 영문(`Packinbag`)으로 최적화하여, 한글 번들/바이너리명 패칭 시 macOS Apple Silicon(ARM64)의 동적 링커(dyld) 및 AMFI 보안 모듈에서 발생하던 Mach-O 헤더 오프셋 오류 및 `Trace/BPT trap: 5` (SIGTRAP, exit code 133) 크래시 원천 해결.
  - 앱 창 제목(`title: "팩인백"`) 및 macOS 메뉴바는 한글 "팩인백"으로 정상 표시 유지.
  - 자체 배포 환경에 맞춰 `identity: null`, `hardenedRuntime: false`, `gatekeeperAssess: false`를 명시하여 라이브러리 검증 충돌 차단.
  - macOS에서 창을 닫은 후 독(Dock) 아이콘 클릭 시 메인 창이 정상 재오픈되도록 개선 (`app.on("activate")`).
- **macOS 배포 포맷 확장 (DMG & ZIP 동시 지원)**:
  - macOS 사용자가 더욱 간편하게 실행할 수 있도록 기존 `.zip` 외에 `.dmg` 디스크 이미지 포맷 동시 배포.
  - GitHub 릴리즈 안내에 macOS Gatekeeper 격리 해제(`xattr -cr`) 팁 추가.

---

## [1.0.12] - 2026-09-21

### 버그 수정 및 개선 사항
- **멀티 디바이스 메모팩 실시간 동기화 복원 및 온/오프라인 파이프라인 통합**:
  - 온라인 로그인 사용자는 PWA/웹/앱/포터블 전 환경에서 100% 원격 Firestore 파이프라인을 사용하도록 `isOfflineEnvironment` 판별 로직 강화 (과거 잔여 오프라인 플래그로 인한 로컬 스토리지 우회 원천 차단).
  - iOS/iPadOS PWA 백그라운드 전환(화면 끄기, 홈 화면 스와이프, 앱 전환) 시 대기 중인 디바운스 자동저장을 동기적으로 즉시 플러시(`visibilitychange`, `pagehide`, `beforeunload`)하여 데이터 유실 방지.
  - `AppShell` 및 메모팩 에디터(`PackNoteEditorScreen`)에 원격 실시간 변경 감지 파이프라인 연동: 다른 기기에서 수정한 최신 내용이 실시간 자동 반영되며, 타이핑 중에는 커서 튕김 방지용 "최신본 반영" 알림 배너 제공.
  - 뒤로가기 버튼 클릭 시 디바운스 대기 없이 즉시 확정 저장 보장.
  - PWA 서비스 워커 캐시 버전 상향(`pib-pwa-v1.0.12`)으로 구버전 캐시 고착 제거 및 네트워크 복구(`online` 이벤트) 시 Firestore 네트워크 연결 강제 재활성화.
- **다운로드 릴리즈 링크 갱신**:
  - v1.0.12 무설치 포터블 배포판(Windows/macOS) 다운로드 링크 갱신.

---

## [1.0.11] - 2026-09-21

### 버그 수정 및 개선 사항
- **온라인 환경 포터블 사용 시 메모팩 및 가방 원격 Firestore 저장 정상화**:
  - 데스크톱 포터블(Electron) 환경에서 실제 Firebase 온라인 계정으로 로그인한 경우, 로컬 스토리지로 우회되지 않고 원격 Firestore 데이터베이스에 메모팩 및 가방 데이터가 정상 저장 및 실시간 동기화되도록 환경 판별 로직(`isOfflineEnvironment`) 개선.
  - 기존에는 데스크톱 포터블 환경이면 온라인 로그인 여부와 무관하게 무조건 오프라인으로 판별되어 로컬 스토리지에만 저장되고 원격 Firestore 저장이 누락되던 문제를 해결.
  - 명시적 오프라인 모드(`pib_offline_mode === 'true'`) 또는 오프라인 게스트 계정(`OFFLINE_USER_UID`)인 경우에만 로컬 오프라인 저장소를 사용하도록 분기 최적화.
- **포터블 환경 원격 API 연동 및 하위 폴더 생성 안정화**:
  - 포터블 환경에서 온라인 계정으로 폴더/팩 생성(`create-library-pack`) 및 가방 생성(`create-bag`) 시 로컬 서버 대신 실제 원격 운영 서버 API를 호출하도록 API 엔드포인트 자동 연결(`getApiUrl`) 적용.
  - Electron 창에서 원격 API와 통신할 수 있도록 `webSecurity: false` 옵션 적용.
  - 사이드바에서 폴더 하위에 새 팩/폴더 생성 시 부모 폴더가 자동으로 펼쳐져 생성 결과가 트리에 즉시 표시되도록 개선.
- **다운로드 릴리즈 링크 갱신**:
  - v1.0.11 무설치 포터블 배포판(Windows/macOS) 다운로드 링크 갱신.

---

## [1.0.8] - 2026-09-18

### 버그 수정 및 개선 사항
- **온/오프라인 하이브리드 로그인 및 세션 동기화 안정화**:
  - 온라인 로그인 시도(`signInWithEmail`, `signInWithGoogle` 등) 시 로컬에 남아있던 오프라인 모드 플래그(`pib_offline_mode`)를 즉시 자동 초기화하여 온라인 모드로 전환 보장.
  - `onAuthStateChanged`에서 유효한 로그인 계정 세션 감지 시 기존 오프라인 플래그보다 실제 계정 세션을 최우선 적용하여, 폐쇄망에서 오프라인 모드를 사용한 기기에서도 인터넷 연결 시 온라인 로그인이 정상 진입되도록 개선.
  - 오프라인 시작(1-1, 1-2), 도중 인터넷 단절(2-1), 오프라인 후 온라인 로그인 및 자동 동기화(3-1, 3-2) 3대 시나리오 완전 지원.
- **다운로드 릴리즈 링크 갱신**:
  - v1.0.8 무설치 포터블 배포판(Windows/macOS) 다운로드 링크 갱신.

---

## [1.0.7] - 2026-09-18

### 버그 수정 및 개선 사항
- **포터블 데스크톱 기존 오프라인 로컬 저장소 호환성 복원**:
  - Electron 접속 호스트를 기존 포터블 사용자들이 사용하던 `127.0.0.1`(`http://127.0.0.1:${port}`)로 복원.
  - 브라우저 도메인(Origin) 분리 정책으로 인해 `localhost` 환경에서 이전 오프라인 가방/아이템 데이터가 조회되지 않던 현상을 해결하여, 기존 폐쇄망 및 오프라인에서 작성한 로컬 데이터가 그대로 유지·표시되도록 개선.
  - Firebase 콘솔의 `127.0.0.1` 승인 도메인 등록과 연동되어 온라인 로그인 및 백그라운드 자동 동기화도 정상 지원.
- **다운로드 릴리즈 링크 갱신**:
  - v1.0.7 무설치 포터블 배포판(Windows/macOS) 다운로드 링크 갱신.

---

## [1.0.6] - 2026-09-17

### 신규 기능 및 개선 사항
- **데스크톱 모드 팩 보관함 폴더 뷰 (PackFolderView) 신규 도입**:
  - 좌측 사이드바에서 팩 보관함 폴더(예: 여행가방) 클릭 시, 우측 넓은 화면에 하위 팩들이 2~3열 카드 그리드로 시원하게 펼쳐지는 전용 뷰 제공.
  - 폴더 뷰 내에서 팩 아이템 인라인 체크/해제, 아이템 삭제, 하단 인라인 빠른 추가 기능 지원.
  - 상단 브레드크럼(경로 이동) 및 하위 폴더 타일(중첩 폴더 진입) 지원으로 직관적인 계층 탐색 지원.
- **서비스 용어 '짐' -> '아이템' 일괄 변경**:
  - 화면 전반의 UI 텍스트(플레이스홀더, 버튼, 필터, 모달, 토스트, 설정 화면 등) 및 코드 주석의 '짐'을 '아이템'으로 교체.
  - '화면 꺼짐 방지', '펼쳐짐', '빠짐없이', '짐작' 등 용언 활용형 및 동음이의어는 원형 그대로 안전하게 보존.
- **오프라인 무설치 실행파일명 변경**:
  - Electron 배포 설정을 수정하여 Windows 실행 파일명을 `팩인백.exe` (`Pack In Bag.exe` -> `팩인백.exe`)로 변경 및 안내 문서 동기화.

---

## [1.0.5] - 2026-09-17

### 버그 수정 및 개선 사항
- **포터블 데스크톱 온/오프라인 하이브리드 지원 및 로그인 정상화**:
  - Electron 메인 프로세스 접속 호스트를 `localhost`(`http://localhost:${port}`)로 표준화하여 Firebase 기본 승인 도메인 호환성 확보.
  - 앱 시작 시 인터넷 유무에 따른 `?offline=true` 강제 URL 파라미터 주입을 제거하여, PWA와 마찬가지로 브라우저 내장 세션 및 Firestore 캐시(`persistentLocalCache`)가 스스로 오프라인/온라인 상태를 전환하도록 개선.
  - 마운트 직후 비동기 세션 복원이 완료되기 전에 섣부르게 오프라인 모드로 덮어쓰던 경합 조건(Race Condition)을 제거하고, 세션 검증 완료 후 비로그인 사용자만 안전하게 로컬 오프라인 모드로 진입하도록 정돈.
  - 기존 로그인 사용자는 오프라인 환경에서도 캐시된 가방/팩 데이터를 끊김 없이 조회/수정할 수 있으며, 인터넷 재연결 시 백그라운드에서 자동 동기화 지원.
- **인증 에러 메시지 매핑 보강**:
  - `auth/unauthorized-domain` 에러 발생 시 사용자 친화적인 안내 메시지("승인되지 않은 도메인 환경이에요. Firebase 콘솔 설정을 확인하거나 오프라인 모드를 이용해주세요.") 추가.
- **다운로드 릴리즈 링크 갱신**:
  - v1.0.5 무설치 포터블 배포판(Windows/macOS) 다운로드 링크 갱신.

---

## [1.0.4] - 2026-09-16

### 신규 기능 및 개선 사항
- **PWA 오프라인 App Shell 캐싱 지원**:
  - 서비스 워커(`public/sw.js`) 캐싱 엔진 도입으로 비행기 모드 및 인터넷 단절 환경에서도 404 없이 PWA 즉시 실행.
  - HTML 문서는 `Network-First with Cache Fallback`, Next.js 정적 번들 및 에셋은 `Cache-First` 적용.
  - API 통신 및 Firebase 동기화는 실시간 네트워크 전용으로 유지하여 캐시 고착 방지.
- **온/오프라인 하이브리드 데이터 연동 (2-Track 아키텍처)**:
  - Firebase 내장 IndexedDB 캐시(`persistentLocalCache`) 연동: 오프라인 진입 시 기존 로그인 사용자를 강제 로그아웃시키지 않고 세션을 유지하여, 비행기 안에서도 기존에 싸둔 가방과 체크리스트가 그대로 열리고 체크/수정 가능.
  - 비행기에서 체크/수정한 내역은 착륙 후 온라인 연결 시 Firebase SDK를 통해 서버로 자동 업로드(동기화).
  - 비로그인/신규 기기에서 오프라인 접속 시 오프라인 게스트 모드로 자동 진입 및 스플래시 안전 타임아웃 적용.
  - 오프라인 상태에서 새 가방/팩 생성 시 로컬 보관함에 안전하게 생성하고 온라인 복귀 후 계정으로 가져오기 지원.
- **가방보관함 가방 카드(BagCard) UI 가독성 개선**:
  - 카드 하단에 중복 표시되던 원형 도넛 게이지(`ProgressRing`) 및 아이템 개수 수치(예: `11/16`) 제거.
  - 카드 맨 밑바닥 2.5px 테마 색상 가로 진행률 바만 깔끔하게 유지하여 모바일 2열 화면에서 시원한 여백과 가독성 확보.
- **데스크톱 및 다운로드 릴리즈 링크 갱신**:
  - v1.0.4 오프라인 무설치(Portable) 배포판 갱신 및 다운로드 링크 반영.

---

## [1.0.3] - 2026-09-09

### 신규 기능
- **개인 업무 관리 기능 추가 (칸반보드로 시작하기)**:
  - 가방 생성 시 [칸반보드로 시작하기] 옵션 추가 (온라인 및 오프라인 데스크톱 지원).
  - 5개 기본 팩 자동 구성: 메모팩 [업무노트], 체크리스트팩 [대기], [진행중], [완료], [보류].
  - 칸반보드 가방 내부 특화: "팩 불러오기" 및 체크리스트 보관함 연동 숨김 처리 (메모팩 동기화는 유지).
  - 완료 팩 시각적 뱃지(`[완료 팩]`) 표시 및 팩 메뉴에서 언제든 다른 팩을 "완료 팩으로 지정" 가능.
  - **완료 항목 자동 이동**: 상단 툴바/보기메뉴 옵션을 켜두면 다른 팩에서 항목 체크 시 [완료 팩]으로 즉시 자동 이동.
  - **완료 팩 안전 삭제**: 완료 팩 삭제 시 다른 체크리스트 팩을 새 완료 팩으로 지정하도록 안전하게 유도하는 모달 적용.
- **앱 실행 시 오늘 마감 업무 알림**:
  - 설정 > 가방 설정에 "앱 실행 시 오늘 마감 내용 보기" 토글 추가.
  - 앱 실행 시 오늘 마감인 항목이 있으면 모아보기 팝업 모달(`TodayTasksModal`)을 띄워 한눈에 확인하고 해당 가방으로 바로 이동 가능.

### 개선 사항
- **가방보관함 상단 필터 및 모바일 짤림 개선**:
  - 1단: `[전체] | [진행중] | [업무] | [보관]` 4개 세그먼트 탭 필터 분리 적용.
  - 2단: `[코드 참여]`(좌) + `[정렬] [1열/2열]`(우)로 분리하고 아이콘 제거 및 여백 최적화로 모바일 화면 짤림 완벽 해결.
- **팩 새로고침 및 저장 버튼 단일화**:
  - 기존의 새로고침 버튼을 제거하고 저장 버튼 하나로 통합.
  - 보관함 원본과 내용이 다를 경우 저장 아이콘에 빨간 점 표시.
  - 저장 버튼 클릭 시: [보관함에서 다시 불러오기] / [보관함 원본에 덮어쓰기] / [새로운 팩으로 저장] 팝업 제공.
- **데스크톱 가방 생성 옵션 통일**:
  - 데스크톱 모드에서도 가방 생성 시 [직접 만들기 / 칸반보드로 시작하기 / 클립보드에서 가져오기] 모달이 동일하게 노출되도록 개선.

### 제거 및 정리
- 레거시 기능("샘플로 시작하기", "스프레드시트/엑셀 링크") 컴포넌트 및 로직 완전 삭제.

---

## [1.0.2] - 2026-09-08

### 개선 사항
- **폐쇄망/사내망 오프라인 자동 감지 신뢰성 개선**:
  - Firebase 네트워크 타임아웃 단축 및 오프라인 감지 플래그 로컬 캐싱 적용.
  - 앱 구동 시 로그인 화면 깜빡임 현상 차단 및 오프라인 모드 즉시 진입.
- 최신 릴리즈 v1.0.2 무설치 포터블 패키징 및 다운로드 링크 갱신.

---

## [1.0.1] - 2026-09-08

### 신규 기능 및 개선 사항
- **메모팩 기능 강화**: 메모팩 코드블럭 접기/펼치기 기능 추가.
- **포터블 오프라인 지원**: 포터블 오프라인 자동 감지 및 초기 데이터 가져오기 모달 연동.
- **시작페이지 설정 개선**: 시작페이지 설정을 기기별 로컬 스토리지 독립 저장으로 전환.
- **문구 일반화**: 링크 편집 및 단축 URL 모달 예시 문구 일반화.
- README 상단 최신 릴리즈 Assets 다운로드 테이블 및 뱃지 추가.

---

## [1.0.0] - 2026-09-08

### 최초 릴리즈
- 팩인백 데스크톱 오프라인 무설치(Portable) 배포판 최초 출시.
- 인터넷/네트워크 연결 없이 로컬 PC에서 가방과 팩 관리 가능.
- 로컬 스토리지 기반 데이터 저장 및 JSON 백업/복원 지원.
