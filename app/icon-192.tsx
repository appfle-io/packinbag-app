// [사용 안 함 - 삭제해도 됨]
// Next.js는 app/ 폴더에서 "icon.tsx"라는 정확한 이름만 특수 파일로 인식하고
// "icon-192.tsx" 같은 임의 이름은 라우트로 연결해주지 않는다는 걸 나중에 확인함.
// 그래서 192 사이즈 아이콘은 app/icon-192/route.tsx (Route Handler)로 다시 만들었음.
// 이 파일은 죽은 코드라 빌드에 영향 없지만, 터미널에서 아래처럼 지워도 무방:
//   rm app/icon-192.tsx
export {};
